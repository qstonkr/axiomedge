"""Vision OCR Fallback Provider -- REMOVED.

This module has been intentionally emptied. The fallback OCR provider
is no longer used; errors from PaddleOCR should surface directly.
"""

raise ImportError(
    "VisionFallbackProvider has been removed. Use PaddleOCRProvider directly."
)
