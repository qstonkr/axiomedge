import { ConfigClient } from "@/components/admin/ConfigClient";

export const metadata = { title: "가중치 설정 · axiomedge admin" };

export default function AdminConfigRoute() {
  return <ConfigClient />;
}
