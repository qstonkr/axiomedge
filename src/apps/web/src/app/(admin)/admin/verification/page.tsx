import { VerificationClient } from "@/components/admin/VerificationClient";

export const metadata = { title: "검증 대기 · axiomedge admin" };

export default function AdminVerificationRoute() {
  return <VerificationClient />;
}
