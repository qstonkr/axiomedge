import { GlossaryClient } from "@/components/admin/GlossaryClient";

export const metadata = { title: "용어집 · axiomedge admin" };

export default function AdminGlossaryRoute() {
  return <GlossaryClient />;
}
