import { QualityClient } from "@/components/admin/QualityClient";

export const metadata = { title: "RAG 품질 · axiomedge admin" };

export default function AdminQualityRoute() {
  return <QualityClient />;
}
