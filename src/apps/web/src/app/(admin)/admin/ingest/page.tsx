import { IngestClient } from "@/components/admin/IngestClient";

export const metadata = { title: "Ingest 작업 · axiomedge admin" };

export default function AdminIngestRoute() {
  return <IngestClient />;
}
